
// Auth Check Dummy for Local Development
// Always allows access without redirecting to CTA

console.log("🔓 Auth Check Bypass Active (Local Dev)");

window.isUserAuthenticated = function () {
    return true;
};

// Mock any other global functions that auth-check might provide
window.checkAuth = function () {
    console.log("Checking auth... Valid!");
    return true;
};