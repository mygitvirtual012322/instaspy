providers = ["python"]

[phases.setup]
nixPkgs = ["python3"]

[phases.install]
cmds = ["pip install -r stalkea_clone/requirements.txt"]

[start]
cmd = "gunicorn server:app --bind 0.0.0.0:$PORT --workers 1 --threads 4"
